<!-- Comments Form -->
<div class="card my-4">
    <h5 class="card-header card-header-sm"><i class="fas fa-comment"></i> Laisser un commentaire :</h5>
    <div class="card-body card-body-sm">
        <form action="/post/{{ post.id }}/{{ post.slug }}/new-comment" method="post">
            <div class="form-group">
                <textarea class="form-control {% if session['checkPostComment'] %}is-invalid{% endif %}" rows="5" name="content" id="commentForm"></textarea>
                <div class="invalid-feedback">
                    {{ session['checkPostComment'] }}
                </div>
            </div>
            <button type="submit" class="btn btn-primary">Envoyer</button>
        </form>
    </div>
    <div class="card-footer text-danger font-weight-bold">
        Votre commentaire devra être validé par la modération avant d'être publié !
    </div>
</div>